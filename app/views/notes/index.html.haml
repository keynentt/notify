.wrapper_with_padding
	#notes.clearfix
		- unless @notes.blank?
			- @notes.each do |note|
				%a{ href: (url_for [note])}
					.note
						%p.title= note.title
						%p.date= note.created_at.strftime("%B, %d, %Y") 
		- else
			%h2 Add a note
			%p It appears you haven't created any notes yet. Why don't you create a new note below.
			%button= link_to "New Note", new_note_path




	